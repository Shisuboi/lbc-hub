# Purge auto 30j — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Supprimer automatiquement les opportunités de plus de 30 jours (hors favoris) au démarrage du moteur `--auto`, pour protéger le free tier Supabase.

**Architecture:** Un nouveau module `engine/maintenance.py` expose `run_maintenance(supa, cfg)` et `purge_old_opportunities(supa, days)`. `server.py` appelle `run_maintenance` une fois au boot, avant de lancer les coroutines permanentes. Deux appels PostgREST : GET favoris protégés → DELETE opportunités trop vieilles (avec exclusion si favoris).

**Tech Stack:** Python 3.12, aiohttp, pytest asyncio_mode=auto. Convention : pytest = backend uniquement ; serveur dev = `python server.py`.

**Spec:** `docs/superpowers/specs/2026-06-04-purge-auto-30j-design.md`

---

## File Structure

| Fichier | Action | Responsabilité |
|---|---|---|
| `engine/maintenance.py` | Créer | `purge_old_opportunities` + `run_maintenance` |
| `.env.example` | Modifier | Documenter `PURGE_DAYS` |
| `server.py` | Modifier | Appel `run_maintenance` au boot `--auto` |
| `tests/test_engine_maintenance.py` | Créer | 4 tests (TDD) |

---

## Task 1 : `engine/maintenance.py` (TDD)

**Files:**
- Create: `engine/maintenance.py`
- Create: `tests/test_engine_maintenance.py`

- [ ] **Step 1 : Écrire les tests**

Créer `tests/test_engine_maintenance.py` :

```python
import pytest
from aiohttp import web, ClientSession
from engine.supa import Supa
from engine.maintenance import purge_old_opportunities, run_maintenance


# ── Helpers ──────────────────────────────────────────────────────────────────

def _make_app(favorites: list, n_deleted: int) -> tuple[web.Application, dict]:
    """Crée une app aiohttp qui simule PostgREST pour la purge."""
    captured = {}

    async def get_favs(request):
        return web.json_response(favorites)

    async def delete_opps(request):
        captured["params"] = dict(request.query)
        return web.Response(
            status=204,
            headers={"Content-Range": f"*/{n_deleted}"},
        )

    app = web.Application()
    app.router.add_get("/rest/v1/item_favorites", get_favs)
    app.router.add_delete("/rest/v1/opportunities", delete_opps)
    return app, captured


# ── Tests ─────────────────────────────────────────────────────────────────────

async def test_purge_no_favorites(aiohttp_server):
    """0 favori → DELETE sans filtre id=not.in., retourne le nombre supprimé."""
    app, captured = _make_app(favorites=[], n_deleted=3)
    server = await aiohttp_server(app)
    async with ClientSession() as session:
        supa = Supa(str(server.make_url("")), "fake-key", session)
        n = await purge_old_opportunities(supa, days=30)
    assert n == 3
    assert "id" not in captured.get("params", {}), "ne doit pas avoir de filtre not.in. sans favori"


async def test_purge_with_favorites(aiohttp_server):
    """Avec favoris → DELETE exclut leurs IDs via id=not.in.(…)."""
    favs = [{"opportunity_id": "aaa"}, {"opportunity_id": "bbb"}]
    app, captured = _make_app(favorites=favs, n_deleted=7)
    server = await aiohttp_server(app)
    async with ClientSession() as session:
        supa = Supa(str(server.make_url("")), "fake-key", session)
        n = await purge_old_opportunities(supa, days=30)
    assert n == 7
    id_param = captured.get("params", {}).get("id", "")
    assert "not.in." in id_param
    assert "aaa" in id_param
    assert "bbb" in id_param


async def test_purge_nothing_to_delete(aiohttp_server):
    """0 ligne à supprimer → retourne 0 sans erreur."""
    app, captured = _make_app(favorites=[], n_deleted=0)
    server = await aiohttp_server(app)
    async with ClientSession() as session:
        supa = Supa(str(server.make_url("")), "fake-key", session)
        n = await purge_old_opportunities(supa, days=30)
    assert n == 0


async def test_run_maintenance_resilient(aiohttp_server, monkeypatch):
    """run_maintenance absorbe les exceptions : ne lève jamais, logue l'erreur."""
    async def _fail(supa, days):
        raise RuntimeError("réseau mort")
    monkeypatch.setattr("engine.maintenance.purge_old_opportunities", _fail)

    # supa factice (jamais appelé vu le monkeypatch)
    app, _ = _make_app(favorites=[], n_deleted=0)
    server = await aiohttp_server(app)
    async with ClientSession() as session:
        supa = Supa(str(server.make_url("")), "fake-key", session)
        # Ne doit PAS lever
        await run_maintenance(supa, cfg={})
```

- [ ] **Step 2 : Lancer → échec**

```bash
python -m pytest tests/test_engine_maintenance.py -v
```

Expected : `ModuleNotFoundError: No module named 'engine.maintenance'`

- [ ] **Step 3 : Créer `engine/maintenance.py`**

```python
"""Tâches de maintenance du moteur autonome (exécutées au démarrage --auto).

Actuellement : purge des opportunités > PURGE_DAYS jours, sauf celles en favori.
Extensible pour de futures tâches (recalcul stats, flush outbox…).
"""
from __future__ import annotations

from datetime import datetime, timedelta, timezone


async def purge_old_opportunities(supa, days: int) -> int:
    """Supprime de Supabase les opportunités de plus de `days` jours (hors favoris).

    Retourne le nombre de lignes supprimées.
    Lève une exception en cas d'erreur réseau (l'appelant décide de la gestion).
    """
    threshold = (datetime.now(timezone.utc) - timedelta(days=days)).isoformat()

    # Étape 1 : IDs protégés (en favori)
    url_fav = f"{supa.base}/rest/v1/item_favorites"
    async with supa.session.get(
        url_fav, params={"select": "opportunity_id"}, headers=supa._headers()
    ) as resp:
        resp.raise_for_status()
        favs = await resp.json()

    protected_ids = [f["opportunity_id"] for f in favs]

    # Étape 2 : DELETE opportunités trop vieilles
    url_del = f"{supa.base}/rest/v1/opportunities"
    params: dict = {"created_at": f"lt.{threshold}"}
    if protected_ids:
        params["id"] = f"not.in.({','.join(protected_ids)})"

    headers = supa._headers({"Prefer": "count=exact"})
    async with supa.session.delete(url_del, params=params, headers=headers) as resp:
        resp.raise_for_status()
        cr = resp.headers.get("Content-Range", "*/0")  # ex. "*/42"
        try:
            n = int(cr.split("/")[-1])
        except (ValueError, IndexError):
            n = 0

    return n


async def run_maintenance(supa, cfg: dict) -> None:
    """Point d'entrée maintenance au démarrage du moteur --auto.

    Best-effort : une erreur est loguée mais ne bloque pas le démarrage.
    """
    days = int(str(cfg.get("PURGE_DAYS", "30")))
    try:
        n = await purge_old_opportunities(supa, days)
        print(f"[maintenance] purge : {n} opportunité(s) supprimée(s) (>{days}j, hors favoris)")
    except Exception as exc:
        print(f"[maintenance] purge échouée (non bloquant) : {exc}")
```

- [ ] **Step 4 : Lancer → succès**

```bash
python -m pytest tests/test_engine_maintenance.py -v
```

Expected : 4 tests PASS.

- [ ] **Step 5 : Non-régression**

```bash
python -m pytest tests/ -q
```

Expected : tous les tests passent.

- [ ] **Step 6 : Commit**

```bash
git add engine/maintenance.py tests/test_engine_maintenance.py
git commit -m "feat(engine): maintenance.py — purge opportunites >30j hors favoris"
```

---

## Task 2 : `.env.example` — documenter `PURGE_DAYS`

**Files:**
- Modify: `.env.example`

- [ ] **Step 1 : Ajouter la variable**

Dans `.env.example`, remplacer la ligne :

```
# Optionnel (Phase C) : TELEGRAM_BOT_TOKEN=...
```

par :

```
# Optionnel (Phase C) : TELEGRAM_BOT_TOKEN=...
# PURGE_DAYS=30   # jours avant purge des opportunités au démarrage du moteur (défaut : 30)
```

- [ ] **Step 2 : Commit**

```bash
git add .env.example
git commit -m "chore: documente PURGE_DAYS dans .env.example"
```

---

## Task 3 : Brancher `run_maintenance` dans `server.py`

**Files:**
- Modify: `server.py`

- [ ] **Step 1 : Ajouter l'import**

Dans `server.py`, après la ligne :

```python
from engine.telemetry import heartbeat_worker
```

ajouter :

```python
from engine.maintenance import run_maintenance
```

- [ ] **Step 2 : Appeler `run_maintenance` au boot**

Dans `start_autonomous_engine`, remplacer :

```python
    stop_event = asyncio.Event()
    app["engine_stop"] = stop_event
    app["engine_brain"] = brain
```

par :

```python
    stop_event = asyncio.Event()
    app["engine_stop"] = stop_event
    app["engine_brain"] = brain

    # Maintenance au démarrage : purge des opportunités > PURGE_DAYS jours (hors favoris).
    await run_maintenance(supa, cfg)
```

- [ ] **Step 3 : Vérifier le démarrage**

Lancer le serveur en mode normal (sans `--auto`) et vérifier qu'il démarre sans erreur :

```bash
python server.py
```

Expected : démarrage normal, aucun `[maintenance]` dans les logs (le bloc ne s'exécute que sous `--auto`).

> Note : pour voir le log `[maintenance]` en action, il faut `python server.py --auto` avec un `.env` valide. La purge s'exécute au boot et loggue le nombre de lignes supprimées (ou 0 si rien à purger).

- [ ] **Step 4 : Non-régression finale**

```bash
python -m pytest tests/ -q
```

Expected : tous les tests passent.

- [ ] **Step 5 : Commit**

```bash
git add server.py
git commit -m "feat(server): appel run_maintenance au demarrage --auto (purge 30j)"
```

---

## Validation finale

1. `python -m pytest tests/test_engine_maintenance.py -v` → 4 PASS
2. `python -m pytest tests/ -q` → suite complète PASS
3. (optionnel, si moteur disponible) `python server.py --auto` → log `[maintenance] purge : N opportunité(s) supprimée(s)`

---

## Self-Review

**Couverture spec :**
- `run_maintenance(supa, cfg)` best-effort → Task 1 ✅
- `purge_old_opportunities` : GET favoris → DELETE avec `lt.{threshold}` [+ `not.in.`] → Task 1 ✅
- `Prefer: count=exact` + parse `Content-Range` → Task 1 ✅
- `PURGE_DAYS` configurable depuis `.env` → Task 1 (lu dans `run_maintenance`) + Task 2 ✅
- Appel au boot `--auto` avant `asyncio.gather` → Task 3 ✅
- 4 cas de tests (0 favori, N favoris, 0 supprimé, résilience) → Task 1 ✅

**Types/signatures cohérents :**
- `purge_old_opportunities(supa, days: int) -> int` défini Task 1, monkeypatché Task 1 test 4 ✅
- `run_maintenance(supa, cfg: dict) -> None` défini Task 1, appelé Task 3 ✅
- `supa._headers(extra)` : méthode existante de `Supa` (vérifiée dans `engine/supa.py`) ✅
- `supa.base`, `supa.session` : attributs existants de `Supa` ✅
